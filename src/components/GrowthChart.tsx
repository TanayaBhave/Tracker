// Growth charts: WHO weight-for-age at corrected age (live) + Fenton 2013 (historical).
// Stub frozen in Phase 2; implemented by workstream W5 (Phase 3).
export function GrowthChart() {
  return (
    <div className="empty">
      Growth charts arrive in the next update.<br />
      Log weights now (including past ones) — they'll appear on the chart automatically.
    </div>
  );
}
