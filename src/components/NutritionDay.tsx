// Daily nutrition view: macro pie + micronutrient %-of-DRI bars.
// Stub frozen in Phase 2; implemented by workstream W4 (Phase 3).
export function NutritionDay() {
  return (
    <div className="empty">
      Nutrition tracking arrives in the next update.<br />
      Keep logging meals with ingredients — the data is already being collected.
    </div>
  );
}
