// USDA FoodData Central proxy — implemented in Phase 3 by workstream W4.
// Registered now (empty) so index.js never needs to change when that lands.
export default async function usdaRoutes(_fastify) {
  // Phase 3 (W4): USDA FDC proxy
}
